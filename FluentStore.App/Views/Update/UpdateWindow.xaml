<winuiex:WindowEx
    x:Class="FluentStore.Views.Update.UpdateWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentStore.Views.Update"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    mc:Ignorable="d"
    Title="Update">

    <winuiex:WindowEx.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </winuiex:WindowEx.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Rectangle Grid.RowSpan="2" Fill="{ThemeResource SystemControlBackgroundAltMediumLowBrush}"/>

        <TextBlock Text="{x:Bind ViewModel.Package.PackageHandler.DisplayName}"
                   Padding="8,4,0,0" FontSize="12" />

        <TextBlock x:Name="HeaderBlock" Text="Do you want to download the latest release?"
                   Grid.Row="1" Padding="20,0,20,12" FontWeight="SemiBold" FontSize="20"
                   TextWrapping="WrapWholeWords" />

        <ProgressBar Grid.Row="2" x:Name="DownloadProgressBar"
                     Visibility="Collapsed" VerticalAlignment="Top" />

        <TextBlock x:Name="StatusBlock" Grid.Row="2" Padding="20,20,20,0"
                   VerticalAlignment="Top" Visibility="Collapsed" TextWrapping="WrapWholeWords" />

        <local:UpdateWindowBody Grid.Row="3" Padding="20" ViewModel="{x:Bind ViewModel}" />

        <Grid Grid.Row="4" Padding="20" ColumnSpacing="12" Background="{ThemeResource SystemControlBackgroundAltMediumLowBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button x:Name="AffirmativeButton" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Content="Download" Click="AffirmativeButton_Click" />
            <Button x:Name="NegativeButton" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Content="Close" Click="NegativeButton_Click" />
        </Grid>

    </Grid>
</winuiex:WindowEx>
