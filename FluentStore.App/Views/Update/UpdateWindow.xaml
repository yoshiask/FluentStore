<winuiex:WindowEx
    x:Class="FluentStore.Views.Update.UpdateWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentStore.Views.Update"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    mc:Ignorable="d"
    Title="Update">

    <winuiex:WindowEx.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </winuiex:WindowEx.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Rectangle Grid.RowSpan="2" Fill="{ThemeResource SystemControlBackgroundAltMediumLowBrush}"/>

        <TextBlock Text="{x:Bind ViewModel.Package.PackageHandler.DisplayName}"
                   Padding="8,4,0,0" FontSize="12" />

        <TextBlock Text="Do you want to download the latest release?"
                   Grid.Row="1" Padding="20,0,20,12" FontWeight="SemiBold" FontSize="20"
                   TextWrapping="WrapWholeWords" />

        <ProgressBar Grid.Row="2" x:Name="UpdateBar" Visibility="Collapsed" />

        <local:UpdateWindowBody Grid.Row="2" Padding="20" ViewModel="{x:Bind ViewModel}" />

        <Grid Grid.Row="3" Padding="20" ColumnSpacing="12" Background="{ThemeResource SystemControlBackgroundAltMediumLowBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Content="Download" />
            <Button Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                    Content="Cancel" />
        </Grid>

    </Grid>
</winuiex:WindowEx>
