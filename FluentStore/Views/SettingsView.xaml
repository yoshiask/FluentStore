<Page
    x:Class="FluentStore.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentStore.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
	DataContext="{StaticResource settings}">

	<ScrollViewer Padding="8">
		<StackPanel Spacing="4">
            <StackPanel>
                <CheckBox Content="Use search exclusion filter" IsChecked="{Binding Default.UseExclusionFilter, Mode=TwoWay}"/>
                <TextBox Text="{Binding Default.ExclusionFilter, Mode=TwoWay}" Header="Search exclusion filter"
                         ToolTipService.ToolTip="A regular expression used to filter out unwanted search suggestions and results."
                         IsEnabled="{Binding Default.UseExclusionFilter}"/>
            </StackPanel>
		</StackPanel>
	</ScrollViewer>
</Page>
